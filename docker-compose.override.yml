# Local dev: expose ports for direct access
version: "3.9"

services:
  paperless:
    ports:
      - "8000:8000"

  qdrant:
    ports:
      - "6333:6333"
      - "6334:6334"  # gRPC

  paperclaw-core:
    ports:
      - "127.0.0.1:8080:8080"
